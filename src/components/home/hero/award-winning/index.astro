---

// IMPORTS ASTRO ATOMS
import HeadingSix from "@/atoms/headings/six/index.astro"
import PictureContain from "@/atoms/picture/contain/index.astro"

// ASTRO PROPS
const {

    CDN,
    data,
    PROJECT

} = Astro.props

// SORTS THE IMAGES ARRAY
const images = data.images.sort( ( a, b ) => a.order - b.order )

---
<section class="space-y-6">
    <HeadingSix>{ data.title }</HeadingSix>
    <div class="grid grid-cols-2 md:flex md:items-center gap-4 md:gap-0">
        {

            images.map( ( value ) => {

                return(
                    <div class="h-16 md:h-20 w-28 md:w-32">
                        <PictureContain
                            alternative_text={ data.title }
                            CDN={ CDN }
                            image_width="200"
                            PROJECT={ PROJECT }
                            source={ value.file }
                        />
                    </div>
                )

            })

        }
    </div>
</section>
